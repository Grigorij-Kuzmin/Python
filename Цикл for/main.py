# Цикл for
# Используется, когда заранее известно кол-во итераций

# range(начало, [конец], [шаг])

for i in range(10):
    # Вывод с переносом строки
    print(i)
    # Вывод без переноса строки (в строчку через пробел)
    # print(i, end=' ')